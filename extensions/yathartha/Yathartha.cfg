\* TLC configuration for small-model verification of Yathartha invariants.
SPECIFICATION Spec

\* Minimal model — sufficient to demonstrate N-17 (battery maintenance)
\* and N-18 (surface integrity). Smaller sets keep TLC tractable; the
\* invariants are universal and don't require larger fan-out.
CONSTANTS
    Agents = {a1}
    Regions = {r1, r2}
    BatteryVersions = {1, 2}
    RefreshCadence = 3
    AcceptanceThreshold = 85

INVARIANTS
    TypeOK
    \* N-16 (CoverageConditionalDrift) is structural — see Yathartha.tla
    \* comment block. Action guard on RaiseDrift is the operative
    \* enforcement; not a TLC-checked state invariant.
    ProbeBatteryMaintenance
    CapabilitySurfaceIntegrity
